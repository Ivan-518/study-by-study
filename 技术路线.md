# 个人 AI 学习平台：桌面应用技术路线

## 1. 技术定位

产品首版是一个仅在个人 Windows 电脑上运行的桌面应用，而不是部署到公网的网页服务。

用户从开始菜单打开应用，在一个原生窗口中学习、搜索资料、记录笔记并与 AI 导师交互。学习数据、导入资料和索引默认只保存在本机；只有“同步最新技术资料”或“调用云端 AI”时，应用才会直接从这台电脑访问外部网络。

> 本地运行不等于完全离线：获取最新资料、使用 GitHub / arXiv、或调用云端模型仍需要网络，但不会经过自建云端服务器。

## 2. 已确认的技术基线

| 层级 | 选择 | 角色 |
| --- | --- | --- |
| 桌面运行时 | Tauri 2 | 将前端打包为 Windows 桌面应用，并提供窗口、菜单、文件、通知与受控系统能力。 |
| 前端 | Vue 3 + Vite + TypeScript | 构建本地桌面应用中的交互界面。 |
| UI 组件 | PrimeVue 4 | 提供表单、树、列表、对话框、虚拟滚动等通用组件；以 unstyled / token 化方式接入。 |
| 客户端状态 | Pinia | 管理跨界面的学习会话、AI 流式状态和 UI 状态。 |
| 本地后端 | Rust + Tauri Commands / Events | 封装业务逻辑、数据库、网络、文件、AI 与后台任务。 |
| 数据库 | SQLite | 单文件保存个人内容、笔记、学习进度、任务和元数据。 |
| 关键词检索 | SQLite FTS5 | 提供本地全文搜索、结果高亮与 BM25 相关性排序。 |
| 语义检索 | 后续本地向量索引 | 首版先保证关键词检索与元数据筛选；内容量增长后再引入本地向量扩展。 |
| AI 接入 | Rust Provider Adapter + 云端模型优先 | 首版调用云端模型；业务层不绑定某一供应商，保留后续替换能力。 |

Tauri 本质上托管静态前端资源，官方对 Vue 等 SPA 前端推荐 Vite，而不原生支持 Nuxt 的 SSR 运行模式。因此原先的 Nuxt 4 + Nitro 方案改为 Vue 3 + Vite + Tauri 2 + Rust 本地后端。参考：[Tauri 前端配置](https://v2.tauri.app/start/frontend/)、[Tauri + Vite](https://v2.tauri.app/start/frontend/vite/)。

## 3. 总体架构

```text
┌──────────────────────────────────────────────────────────────┐
│                     Nexus 桌面应用（Windows）                 │
│                                                              │
│  Vue 3 + Vite + PrimeVue 4 + Pinia                           │
│  首页 · 知识地图 · 搜索 · 阅读 · 笔记 · AI 导师               │
│                         │                                    │
│             Tauri Commands / Events（内部 API）              │
│                         │                                    │
│  Rust 应用服务层                                               │
│  learning · content · search · assistant · sync · backup     │
│       │                     │                    │           │
│       │                     │                    └─ 系统文件 / 安全存储
│       │                     └─ 外部来源与模型服务（按需联网） │
│       └─ SQLite + FTS5 + 本地资料目录                         │
└──────────────────────────────────────────────────────────────┘
```

### 边界原则

- 前端负责呈现和交互，不直接执行 SQL、不保存密钥、不直接调用 AI API。
- Rust 是应用的本地后端，负责所有持久化、联网、文件访问与耗时任务。
- 云端没有账户、数据库或后端服务；所有外部请求都从用户电脑发起。
- 通过 Tauri 的显式 command、event 与 capability 权限边界连接前后端，不暴露任意系统能力给页面。

## 4. 前端方案

### 4.1 前端职责

Vue 界面负责以下体验：

- “继续学习”、知识地图、教程阅读、外部资料详情和笔记。
- 搜索输入、筛选、结果列表、来源引用和导入确认。
- AI 导师对话、流式输出、引用展开和代码片段展示。
- 设置页面：来源、模型、网络同步、备份与隐私偏好。

### 4.2 PrimeVue 4 的使用边界

PrimeVue 4 用作可靠的交互基础，不决定产品视觉语言：

- `AutoComplete`：技术、概念和资料搜索建议。
- `Tree` / `TreeTable`：知识地图与概念关系。
- `Tabs`、`Stepper`、`Accordion`：分层教程与章节内容。
- `Dialog`、`Drawer`、`Toast`、`ConfirmDialog`：导入、确认与轻量操作。
- `VirtualScroller`、`DataTable`：资料、笔记、收藏和搜索结果。
- `ProgressBar`、`Skeleton`、`Tag`：进度、加载和内容类型。

阅读画布、代码块、学习卡、来源引用、AI 对话和知识图谱使用自定义 Vue 组件实现。PrimeVue 采用 unstyled / design tokens 方式接入，避免默认后台模板观感。

### 4.3 Pinia Store 设计

| Store | 管理内容 | 不管理内容 |
| --- | --- | --- |
| `learning-session` | 当前学习对象、阅读位置的即时状态、最近访问 | 学习进度的最终存档。 |
| `assistant` | 对话草稿、流式 token、加载状态、当前引用 | 聊天记录的唯一事实来源。 |
| `ui` | 侧栏、阅读布局、主题、临时提示 | 课程、资料或笔记主数据。 |

所有长期数据以 Rust 后端和 SQLite 为准。搜索关键词、筛选和当前路由同步到 URL，以支持返回、刷新与可复现的搜索状态。

## 5. Rust 本地后端

### 5.1 模块划分

| 模块 | 职责 |
| --- | --- |
| `learning` | 教程、章节、练习、继续学习、掌握状态。 |
| `content` | 外部资料导入、正文解析、元数据、来源追溯与去重。 |
| `search` | FTS5 检索、筛选、排序、结果高亮；后续追加语义检索。 |
| `assistant` | 上下文组装、模型调用、引用绑定、流式事件。 |
| `sync` | GitHub、arXiv、官方资料等来源的本地同步任务。 |
| `notes` | 笔记、正文标注、收藏和“待弄懂”问题。 |
| `backup` | 数据库、附件和设置的本地备份与恢复。 |
| `settings` | 模型供应商、来源、网络与个人偏好。 |

### 5.2 前后端内部 API

前端只调用明确的业务 command，不访问数据库细节。例如：

```text
get_continue_learning()
search_local(query, filters)
get_resource(resource_id)
save_note(input)
update_learning_progress(input)
import_resource(url)
sync_sources(source_ids)
ask_assistant(input)
cancel_assistant(request_id)
create_backup(destination)
restore_backup(source)
```

AI 回复和同步进度不轮询数据库，而由 Rust 通过事件推送给前端：

```text
assistant:delta       # 流式回答文本
assistant:citation    # 新增引用资料
assistant:complete    # 回答完成
sync:progress         # 来源同步进度
sync:complete         # 同步结果
```

### 5.3 后台任务

后台任务随桌面应用启动，在本机进程内运行：

- 手动或定期同步已启用来源。
- 解析导入的 URL、Markdown 或文件。
- 规范化 URL、计算内容 hash、合并重复资料。
- 生成摘要、标签、概念关联和学习卡。
- 建立或刷新 FTS5 索引；未来可追加向量化索引。
- 记录任务状态、失败原因和可重试次数。

首版采用“打开应用后按计划执行 + 手动同步”的模式；不要求常驻 Windows 后台服务。后续如确有需要，再启用系统启动或托盘常驻。

## 6. 本地数据与文件

### 6.1 存储位置

数据保存于应用专属的 Windows App Data 目录：

```text
Nexus/
├── nexus.db              # SQLite 主数据库
├── attachments/          # 用户导入的附件、文章快照
├── cache/                # 可安全重新生成的解析与网络缓存
├── backups/              # 用户创建或定期生成的备份
└── logs/                 # 诊断日志，不记录密钥和正文原文
```

### 6.2 核心表

| 实体 | 用途 |
| --- | --- |
| `learning_tracks`、`concepts`、`concept_relations` | AI 应用开发与模型原理的知识地图。 |
| `courses`、`lessons`、`exercises` | 内置教程、章节和练习。 |
| `resources`、`resource_chunks` | 外部资料、正文分段、摘要、来源和时间。 |
| `resource_concepts` | 资料与概念的关联。 |
| `learning_progress` | 阅读位置、状态、掌握度与最近访问。 |
| `notes`、`bookmarks`、`questions` | 笔记、收藏、标注与待理解问题。 |
| `conversations`、`messages`、`citations` | AI 导师对话与可追溯引用。 |
| `sync_jobs`、`sync_runs` | 来源同步与后台任务记录。 |
| `settings` | 个人配置；敏感凭据不存此表。 |

### 6.3 备份与恢复

- 备份以 SQLite 数据库与资料目录为最小完整单位。
- 用户可以从设置中选择备份位置和频率。
- 恢复前先创建当前数据快照，避免误覆盖。
- 模型 API Key 不进入普通备份；恢复后需要重新确认或从系统安全存储读取。

## 7. 本地搜索方案

### 7.1 首版：全文搜索优先

SQLite FTS5 是首版的关键词检索基础。它提供全文索引、搜索片段高亮与 BM25 排序，适合个人学习资料库。参考：[SQLite FTS5 官方文档](https://www.sqlite.org/fts5.html)。

```text
用户查询
  → FTS5：标题、标签、正文、笔记全文匹配
  → 来源 / 类型 / 时间 / 学习主线过滤
  → BM25 排序与匹配片段
  → 结果关联：概念、前置知识、学习状态、原始链接
```

中文资料需要专门处理：根据实际查询效果选择 FTS5 的 trigram tokenizer 或在入库阶段进行分词，不能默认假设英文式空格分词就足够。

### 7.2 后续：语义与混合检索

当本地内容达到足够规模、关键词搜索无法满足“同义问题 / 模糊提问”时，再加入本地 Embedding 与向量检索：

```text
FTS5 关键词候选 + 向量相似度候选
        → 合并、去重、重排
        → 带来源的学习资料与回答上下文
```

首版不引入独立向量数据库，也不承诺一个尚未验证的向量扩展。向量索引的具体实现将在内容量、Windows 打包兼容性和检索质量验证后确定；这能控制桌面安装包复杂度。

## 8. AI 与外部网络

### 8.1 Provider Adapter

Rust 后端定义稳定能力接口，界面与学习业务不绑定任何模型服务：

```text
generate_explanation(input, level, context)
create_learning_card(resource)
answer_with_citations(question, retrieved_context)
create_exercise(concept, difficulty)
review_code_or_plan(input, rubric)
embed(texts)                 # 后续语义检索
search_latest(query)         # 实时技术搜索
```

首版优先使用云端模型 API，以获得更稳定的中文解释、代码能力与长上下文表现；后续可视需要接入本地模型。调用模型服务时，密钥只在 Rust 后端使用，前端永远不可读取。

默认数据边界如下：学习进度、笔记、收藏、已导入资料与本地索引保留在电脑上；只有完成当前请求所必需的提问文本、用户主动选择的资料片段和系统指令会发送给云端模型。应用在首次配置模型时明确展示这一边界。

### 8.2 AI 回答流程

```text
用户提问
  → Rust 判断是否需要本地检索 / 实时外网搜索
  → 获取当前教程、当前资料、笔记与可信来源片段
  → 组装最小必要上下文
  → Provider Adapter 发起流式调用
  → Rust 将文本与引用事件推送到 Vue
  → 保存最终消息、引用与学习上下文到 SQLite
```

### 8.3 网络透明性

- 首次启用来源同步或云端模型时，应用明确显示将访问的服务和可能发送的数据类型。
- “最新技术搜索”仅在用户主动搜索或开启同步任务时联网。
- 稳定基础教程和已导入资料优先走本地搜索，避免不必要的网络和模型成本。
- 用户可分别关闭来源同步、云端模型和遥测；首版默认不收集产品遥测。

## 9. 外部内容同步

### 9.1 首批来源

- 官方模型、框架与开发工具文档 / 发布说明。
- arXiv 论文及元数据。
- GitHub 仓库、Release 和 README。
- 明确授权访问的高质量技术团队文章。

### 9.2 导入流水线

```text
URL / RSS / API / 用户文件
  → 下载与格式校验
  → 提取正文与元数据
  → URL 规范化、内容 hash 去重
  → 保存原始来源与抓取时间
  → 分段、摘要、标签、概念关联
  → 建立 FTS5 索引
```

首版优先接 API、RSS、GitHub Releases 和许可明确的来源；不构建无差别网页爬虫。每条资料必须保留原始链接、来源、发布时间（可得时）和抓取时间。

### 9.3 趋势发现首版

首页的“正在发生”不再消费固定的官方新闻订阅，而是消费本机生成的趋势事件。首批信号由 GitHub 动态仓库检索、GitHub 本周 Trending 和 Hacker News 开发者讨论构成：

GitHub 主题检索与仓库历史快照、GitHub Trending 的本周 star 增量、Hacker News 主题讨论与互动指标，依次经过原始候选入 SQLite、URL / GitHub 仓库标识归并，以及新鲜度、主题相关性、star 增长、讨论度、多源印证评分，最终生成 `Hot / Watch / Candidate` 趋势事件。项目仅因关键词命中而被发现时只能进入观察池；首页只展示有真实增长、Trending 信号、强社区讨论或多源证据的 `Hot` 事件。

GitHub 搜索只用于建立候选池；项目总 star 数不等同于趋势，应用会在本地保存快照后计算后续增长。Hacker News 仅作为开发者讨论信号，事件详情保留对应讨论页和项目原链接。首版不抓取需要登录态、Cookie 或付费 API 的社交平台；Hugging Face 与 arXiv 将在这条可追溯管线稳定后接入。

趋势发现不局限于 Agent、RAG 或 MCP。首版主题策略同时覆盖模型与推理、生成式图像 / 视频、多模态与视觉、语音、机器学习、AI 编程和本地 AI。每个热点或观察项可打开详情，查看项目 / 文章简介、入选依据、学习与实践价值，以及每个评分证据的原始链接。

## 10. 安全与隐私

- 不存在公网 API、云端账户或远程数据库。
- 数据库、附件和缓存只允许通过应用专属目录读写。
- Rust command 对输入参数、URL 协议、重定向与本地地址进行校验，避免导入 URL 导致 SSRF 或读取本机敏感资源。
- 所有 Markdown、代码和外部 HTML 在显示前清洗，避免脚本注入。
- 模型密钥使用系统安全存储或本地加密凭据，不写入前端 bundle、SQLite 普通表、日志或导出备份。
- Tauri 使用系统 WebView；Windows 开发环境需要 Rust、Microsoft C++ Build Tools 与 WebView2。参考：[Tauri Windows 前置条件](https://v2.tauri.app/start/prerequisites/)。

## 11. 建议的项目结构

```text
nexus/
├── src/                         # Vue 页面、组件、composables、Pinia stores
├── src-tauri/
│   ├── src/
│   │   ├── commands/             # Tauri command 定义
│   │   ├── services/             # learning/content/search/assistant/sync
│   │   ├── repositories/         # SQLite 数据访问
│   │   ├── providers/            # AI、GitHub、arXiv 等外部适配器
│   │   ├── jobs/                 # 本地后台任务
│   │   └── security/             # 密钥、路径、网络校验
│   ├── migrations/               # SQLite 迁移
│   └── tauri.conf.json           # 窗口、权限、打包设置
├── public/
├── docs/
└── 产品方案.md
```

## 12. 分阶段实现

### 阶段 1：本地学习闭环

- Tauri 2 + Vue 3 + Vite + PrimeVue 4 + Pinia 的桌面工程骨架。
- SQLite、迁移、内置少量课程、进度、笔记、收藏与继续学习。
- FTS5 本地搜索、资料详情和知识地图。
- 基础 AI 导师：围绕当前教程或资料提问，流式显示结果。
- 设置页：模型配置、数据目录、手动备份。

### 阶段 2：可信外部资料

- GitHub、arXiv 和官方来源接入。
- 本地同步、解析、去重、摘要、概念关联与任务记录。
- 带来源引用的资料页和 AI 回答。
- 用户可控的同步策略与失败重试。

### 阶段 3：自适应学习与语义检索

- “待弄懂”问题驱动的前置知识推荐。
- 资料与知识地图自动关联。
- 在验证本机兼容性后引入向量检索，形成混合搜索。
- 小实验、代码审阅与学习回顾。

## 13. 当前不做

- 云端部署、登录系统、多人协作、社区和课程市场。
- 远程 PostgreSQL、独立向量数据库、微服务和 Kubernetes。
- 强制学习计划、打卡、排行榜与日历任务。
- 无差别全网爬虫或常驻 Windows 服务。
- 首版即支持 macOS、Linux、手机端或多电脑同步。

## 14. 下一步待确认

1. 是否将 Windows 作为首版唯一目标平台。
2. 首个云端模型供应商及其模型选择、计费方式与单次请求成本上限。
3. 首批需要同步的来源清单与同步频率。
4. 是否把“本地备份到指定目录”作为首版必备功能。
